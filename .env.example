# ============================================================
#  ZYSOF Backend — Environment Variables
#  Copy this file to .env and fill in your values.
#  Never commit .env to version control.
# ============================================================

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=zysof_db
DB_USER=postgres
DB_PASSWORD=Sufyan123

# App settings
APP_ENV=development
SECRET_KEY=change_this_to_a_long_random_string
